mixin alert(data_time)
  -if(messages.success)
    div(class="message info alert alert-success" 
        data-time=`${data_time}`
        show-alert)
      span #{messages.success}
      span(close-alert) X